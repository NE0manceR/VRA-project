import React, { Component } from 'react';
import Requirements from '../../components/requirements/Requirements';

// Import custom components

class RequirementsContent extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <Requirements />;
  }
}

export default RequirementsContent;
